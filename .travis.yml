sudo: false

language:
  - cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test

before_install:
  - pip install --user cpp-coveralls

script:
  - make

after_success:
  - coveralls --root .. -E ".*external.*" -E ".*test/.*.cpp.*"

notifications:
  email: false
